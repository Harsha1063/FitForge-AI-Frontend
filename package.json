{
  "name": "fitforge-ai",
  "private": true,
  "packageManager": "npm@10.9.8",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "format": "turbo run format"
  },
  "devDependencies": {
    "turbo": "^2.5.4"
  },
  "dependencies": {
    "lucide-react": "^1.28.0",
    "recharts": "^3.10.1"
  }
}
